[target.x86_64-pc-windows-msvc]
# 强制使用 Rust 内置的 lld-link
linker = "rust-lld.exe"
rustflags = [
    # 1. 开启本机 CPU 专属指令集优化（AVX2/FMA/BMI2 等）
    "-C", "target-cpu=native",
    # 2. 开启 rustc 前端多线程（9800X3D = 8C16T）
    "-Z", "threads=16",
]